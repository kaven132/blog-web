# 2026-08-02 工作日志

## 博客自动发文（daily auto-post）
- 验证可行性并实测通过：直接写库发文链路（插入/按 slug 查询/删除）在真实 DB 上跑通，库内容不变。
- 新建 `scripts/post-article.ts`：复用 Drizzle schema 直接写 SQLite。支持 `--file=<json>` 插入、`--delete --slug=<slug>` 删除。强制 `published:true`，slug 自动去重（逻辑同 `/api/posts` 的 resolveSlug）。
- 创建 WorkBuddy 每日自动化任务：`automation-1785663383199`，名"博客每日自动发文（书籍推荐 + Claude Code skill）"，`FREQ=DAILY;BYHOUR=10;BYMINUTE=0`，状态 ACTIVE，cwds=博客根目录。
  - 内容：AI 原创两篇/天 —— ①书籍推荐(tags 生活/阅读/文学) ②Claude Code/Claude Agent skill 推荐(tags 技术/AI/agent)。
  - 发布命令必须用 Node 23：`E:/devtools/nodejs/node.exe node_modules/tsx/dist/cli.mjs scripts/post-article.ts --file=...`
- 冒烟测试：手动发布两篇样本（含代码块+中文标签）后删除，pipeline 正常；发现并修复 slug 策略。
- 关键决定：slug 改由自动化生成"日期+主题"的 ASCII（如 `book-rec-20260802` / `claude-skill-20260802`），避免中文 slug，URL 更干净且按天天然不重名。原 slugify 保留中文（如 `测试-claude-code-skill-推荐`）虽可用但不美观。
- 注意：正文禁用 Markdown 表格（手写渲染器不支持）。

## 手动真实发布（17:40 用户要求先看效果）
- 用与自动化完全一致的命令真实发了 2 篇并保留：book-rec-20260802（重读《活着》, 生活/阅读/文学, id=19）、claude-skill-20260802（4 个 Claude Code skill, 技术/AI/agent, id=20）。库 11→13。
- 验证：两 slug 可查、published=1、tags JSON 正确；手写 Markdown 渲染器实测——标题/引用/列表/代码块均正常，无表格破版。临时 JSON 已清理（rm 走 safe-delete）。
- 用 Node 23 后台启动 astro dev（`astro dev --host`，端口 4321），首页/列表/两文章页均 HTTP 200，列表页已出现两新标题。文章现在真实可见。
- 结论：自动发文链路完全可用，明天 10:00 自动化将按同样流程每日发文。

## 优化：加长 + 配图（17:46 起）
- 用户反馈文章太短、无图。核查：博客**本就支持图**——`posts.cover_image` 字段（`schema.ts:10`），`PostCard.tsx:32` 列表缩略图 + `[slug].astro:102` 详情页顶部大图都会渲染；正文插图用 `![alt](url)`（渲染器 `markdown.ts:118` 支持）。发文脚本 `post-article.ts` 已支持 `coverImage` 字段（无需改脚本）。
- 选方案：**外链稳定图库轮换**（用户选）。实测验证 13 个 Unsplash 永久 URL 全部 `200 image/jpeg`（6 个阅读/书籍主题 + 7 个技术/AI 主题）。picsum 在沙箱连不通但浏览器端无碍；loremflickr 是 302 跳转型。
- 更新自动化 `automation-1785663383199`：目标字数 1800–2800、结构化模板（书籍：引子→讲什么→摘抄→亮点→相似书单→适合谁；技术：逐个 skill 含代码+踩坑→横向对比→最佳实践）、封面从对应图库池选 + 正文插 1 张图（独立一行）、图库池写死在 prompt 里禁止编造 URL。
- 按新方案**删掉旧两篇(id19/20)重发**：book-rec-20260802(id21, contentLen 1314, cover SET, 内文图 YES)、claude-skill-20260802(id22, contentLen 1292, cover SET, 内文图 YES)。库 13。页面实测：书籍页 2 张 img、skill 页 3 张 img、列表页 2 个 unsplash 封面缩略图。
- 临时 JSON 清理：bash 的 rm 被 safe-delete 以"相对路径"拒绝（沙箱 /e/... vs Windows E:\...），改用 PowerShell 绝对路径 `E:\...` 删除成功。
- 注意：safe-delete 拦截 `rm` 时要求 Windows 绝对路径，删除项目内临时文件用 PowerShell 更稳。

## 升级：ImageGen 本地封面 + humanizer 去味 + web-search 真实化 + 每日一本/一个（18:03 起）
- 用户要求：用内置 ImageGen 替代 Unsplash 外链封面，并接 humanizer + web-search，书籍/技能改为每天各推荐「一本新书 / 一个 skill」且不复重。
- 安装 humanizer（去AI味 v2.1.1，市场 skill_2053082097175687168）到 ~/.workbuddy/skills/；web-search 为内置 skill 直接用。
- ImageGen 实测：直接生成到 `data/uploads/`，参数 output_dir="E:/work/ai project/blog2/data/uploads"，size 1200x800，quality medium。返回 localPath。本次生成 2 张（书籍温暖书本意象 / 技术 AI 代码代理意象），各约 1.1MB PNG。
- 增强 `scripts/post-article.ts`：新增 `resolveCover()`——coverImage 为 http(s) 外链则原样用；为本地文件路径则拷入 `data/uploads/<slug>.<ext>` 并重写 coverImage 为 `/uploads/<slug>.<ext>`（博客 uploads/[...file].ts 路由直接服务，无需登录）。已实测：封面本地化输出 `/uploads/book-rec-20260802.png` 等，页面 <img> 正确，路由 200 image/png。
- 新增 `scripts/_posted.json`（{"books":[...],"skills":[...]}）做去重历史；自动化 prompt 改为：先读 _posted.json 选题（web-search 核实真实书名/作者/出版年、真实 Claude Code skill 官方用法）、写后写回；每天只推一本新书 + 一个 skill，url/书名不重复。
- 自动化 `automation-1785663383199` prompt 全面升级：web-search 真实资料(防幻觉) + 去 AI 味(用 humanizer 或手动规则：避套话、少破折号、长短句交错、第一人称) + ImageGen 生本地封面(失败回退 Unsplash 池) + 每日一本/一个去重 + 保持无表格约束。
- 本次实测：删旧 id21/22，web-search 取《小王子》(圣埃克苏佩里 1943) 与 Claude Code 自定义 slash command 真实资料，重发 book-rec-20260802(id23)、claude-skill-20260802(id24)，封面为本地 ImageGen 图。库 13。页面/列表页均渲染本地封面。临时 JSON 与原始时间戳 PNG 由 safe-delete 移入回收站（工作区已干净）。
- 注意：自动化运行时能否调用 ImageGen / humanizer / web-search 取决于引擎是否暴露这些工具；prompt 已写明"ImageGen 失败走 Unsplash 回退"以保证 pipeline 不中断。正式运行后需观察首次回报确认工具可用性。

## 配图方案再调整：ImageGen 本地生图 → loremflickr 真实照片外链（18:25 起）
- 用户不想用 AI 生成的图片，问有无"找图片"的 skill。确认内置 `web-image-downloader`（v1.4.0）可从网页抓取真实图片到本地，但较重（需给 SSR 图库页、会抓多张需筛选、自动化引擎能否调用待验证）。
- 用户最终选 **loremflickr 标签直链**（推荐）：外链真实 Flickr 照片，按 URL 标签返回（book,reading / ai,code），`?lock=YYYYMMDD` 保证当天图固定、不同天不同。零下载零脚本，自动化最稳。实测 3 个 URL 均 302→200 返回真实图。
- 更新自动化 `automation-1785663383199` prompt 第四步：封面/内文图改用 loremflickr 标签直链（带 lock=当天日期），保留 Unsplash 池作为 loremflickr 不可达时的回退；web-search + humanizer 不变。
- 实测：删旧 AI 封面两篇(id23/24)，重发 book-rec-20260802(id25)、claude-skill-20260802(id26)，封面=loremflickr 外链。页面实测：书籍页 2 张 / skill 页 2 张 / 列表页 2 张封面缩略图均指向 loremflickr。库 13。旧 AI 封面 PNG 与临时 JSON 已清理；`scripts/post-article.ts` 的 resolveCover() 保留（外链原样、本地路径才拷贝），未来若要本地托管图仍可用。

## 字数扩展到约 3000 字（20:00 起）
- 用户要求文章更长到约 3000 字。更新自动化 `automation-1785663383199` prompt：核心目标字数 1500–2200 → **2800–3200（目标约 3000）**，并把两类文章结构拆成更多可写满的小节（书籍：引子/作者背景/讲什么/意象/摘抄/亮点4点/书单/翻译这件小事/联想/适合谁/收尾；技能：痛点/是什么/怎么用4节含代码/真实案例/踩坑4个/适用/进阶/收尾）。
- 实测重发今天两篇为长版：book-rec-20260802(id29) 中文字数 **2629**、claude-skill-20260802(id31) 中文字数 **2071**+真实代码块（总字符 3427）。库 13。
- 发现并修复渲染隐患：项目手写 Markdown 渲染器（markdown.ts）只认三个反引号且**不支持嵌套代码块**（内层 ``` 会提前关闭外层）。技能篇原"命令里嵌套 git log"被展平，git log 行未高亮。修复：拆成两个独立代码块，并在踩坑第4条说明该注意点。已用渲染器直接验证 git log 落回 `<pre>` 内、无破版。
- 预览服务器重启（astro dev, Node23, 4321）。临时 JSON 由 safe-delete 移回收站，工作区干净。
